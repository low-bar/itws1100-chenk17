http://chenk17rpi.eastus.cloudapp.azure.com/iit/
https://github.com/low-bar/itws1100-chenk17


We are to replace the normal lablist with a AJAX version which loads from js reading a json file. There is also a bonus to load rss with ajax as well. First I had to populate my jsonfeed pages split the lablist and rss which is labeled by page and items inside contained their related lines/tags. Using the example JS I copied it over changed the output to be in the format of my LabList. As for the bonus I had help using claude to use JS to make the xml file. 